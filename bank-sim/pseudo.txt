bank sim
bal start = 0
2 threads, use p_thread
cap = 2mill
t1 -> deposit
	2 mill trans
	each time bal % 200 = 0, add bonus 50 (counts as 50 trans)
	cannot exceed cap 
	track bonus awards count
t2 -> withdraw
	perf 2mill withdraw

each do 2000000 trans

#include <pthread.h>
pthread_mutex_t lock;

main
mutex_init(lock)
thread1
thread2

t1
loop 2mill
entry
lock

critical 
bal += 1
if bonus valid, trans += 50

exit

remainder
